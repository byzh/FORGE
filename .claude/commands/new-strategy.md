Create a new financial strategy named $ARGUMENTS inside FORGE.Core.
Steps:
1. Implement IStrategy interface
2. Include typed parameters (target Delta, DTE, underlying)
3. Add a Validate() method checking parameter coherence
4. Unit test covering edge cases (invalid delta, negative DTE, etc.)
